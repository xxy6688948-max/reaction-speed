# 反应速度挑战 ⚡

一个全栈网页游戏，测试你的反应速度，支持全球排行榜。

## 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | HTML + CSS + Canvas |
| 后端 | Node.js + Express |
| 数据库 | SQLite（better-sqlite3） |
| PWA | Service Worker + Web Manifest |

## 项目结构

```
d:/mianshi/
├── server.js          # Express 服务器（端口 3000）
├── db.js              # SQLite 数据库初始化与查询
├── package.json       # 项目依赖
└── public/            # 前端静态文件
    ├── index.html     # 游戏界面
    ├── style.css      # 样式（暗色主题，响应式）
    ├── game.js        # 游戏核心逻辑（Canvas 渲染）
    ├── sw.js          # Service Worker（离线缓存）
    └── manifest.json  # PWA 配置（可安装到手机桌面）
```

## 启动方式

```bash
npm install
node server.js
```

然后打开浏览器访问 **http://localhost:3000**

## 玩法说明

1. 输入昵称，点击 **Start Game**
2. 屏幕显示红色 "Wait..."，等待随机延时（1~4秒）
3. 屏幕变绿并显示 **TAP!**，尽快点击/触摸屏幕
4. 如果提前点击（红色阶段），计为犯规
5. 共 5 轮，取有效轮次的平均反应时间
6. 点击 **Submit Score** 提交成绩到全球排行榜
7. 排行榜按反应时间升序排列（越快越靠前）

## API 接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/leaderboard` | 获取前 20 名排行榜 |
| POST | `/api/score` | 提交成绩 `{name, score}` |

- name：1-20 个字符
- score：1-9999 的整数（毫秒）

## PWA 特性

- 支持离线游玩（Service Worker 缓存静态资源）
- 手机浏览器可"添加到主屏幕"，像原生 App 一样运行
- 响应式设计，适配各种屏幕尺寸

## 部署到 iOS / Google Play 的路径

当前是 PWA 网页应用，要发布到应用商店可以：

1. **Capacitor**（capacitorjs.com）：将网页应用打包为 iOS/Android 原生项目
2. **PWABuilder**（pwabuilder.com）：从 URL 直接生成应用商店包

需要的条件：
- iOS：macOS + Xcode + Apple Developer 账号（$99/年）
- Android：Android Studio + Google Play 开发者账号（$25 一次性）
